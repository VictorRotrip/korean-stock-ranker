# Core data sources
pykrx>=1.0.45
opendartreader>=0.2.1
finance-datareader>=0.9.50

# Historical KRX market cap (point-in-time) is read directly from the
# FinanceData/marcap GitHub dataset
# (https://github.com/FinanceData/marcap), not via PyPI. Yearly Parquet
# files are downloaded on demand into scripts/python/.cache/marcap/ by
# scripts/python/data_sources/marcap_historical.py.
pyarrow>=15

# Database
psycopg2-binary>=2.9.9

# Data processing
pandas>=2.1.0

# Environment
python-dotenv>=1.0.0
